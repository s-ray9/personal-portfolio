---
import { cn } from "@utils/cn";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class={cn(className)}>
  <img src="/favicon.svg" alt="" class="size-full" aria-hidden="true" />
</span>
